# Sequence Diagram — P5 Sub-Process 5.7
## Coordinator Sprint Control

> Endpoints: `POST /api/coordinator/sprints/{sprintId}/refresh`, `GET /api/coordinator/sprints/{sprintId}/overview`
> Issues: #155 (CoordinatorSprintController)
> JWT principal = Staff UUID, role = COORDINATOR
> Pattern: mirrors `SanitizationController.java` exactly

---

### POST /api/coordinator/sprints/{sprintId}/refresh

```mermaid
sequenceDiagram
    actor Coordinator
    participant CoordinatorSprintController
    participant SprintTrackingOrchestrator
    participant SprintRepository
    participant SprintTrackingLogRepository

    Coordinator->>CoordinatorSprintController: POST /api/coordinator/sprints/{sprintId}/refresh
    Note over CoordinatorSprintController: Authorization: Bearer <staff_jwt> role=COORDINATOR
    Note over CoordinatorSprintController: Optional query param: ?force=true

    CoordinatorSprintController->>SprintRepository: findById(sprintId)
    SprintRepository-->>CoordinatorSprintController: Optional<Sprint>

    alt sprint not found
        CoordinatorSprintController-->>Coordinator: 404 {"message": "Sprint not found"}
    end

    CoordinatorSprintController->>SprintTrackingOrchestrator: triggerForSprint(sprintId, force)

    alt force=false AND sprint.endDate >= today
        SprintTrackingOrchestrator-->>CoordinatorSprintController: throw BusinessRuleException
        CoordinatorSprintController-->>Coordinator: 400 {"message": "Sprint has not ended yet — use ?force=true to refresh early"}
    end

    Note over SprintTrackingOrchestrator: Idempotent reset — delete all existing rows before re-fetching
    SprintTrackingOrchestrator->>SprintTrackingLogRepository: deleteBySprintId(sprintId)

    Note over SprintTrackingOrchestrator: Run full pipeline synchronously — same self-proxy REQUIRES_NEW flow as daily run
    SprintTrackingOrchestrator-->>CoordinatorSprintController: SprintRefreshStats { groupsProcessed, issuesFetched, aiValidationsRun }

    CoordinatorSprintController-->>Coordinator: 200 { sprintId, groupsProcessed, issuesFetched, aiValidationsRun, triggeredAt }
```

---

### GET /api/coordinator/sprints/{sprintId}/overview

```mermaid
sequenceDiagram
    actor Coordinator
    participant CoordinatorSprintController
    participant SprintService
    participant SprintRepository
    participant ProjectGroupRepository
    participant SprintTrackingLogRepository
    participant ScrumGradeRepository

    Coordinator->>CoordinatorSprintController: GET /api/coordinator/sprints/{sprintId}/overview
    Note over CoordinatorSprintController: Authorization: Bearer <staff_jwt> role=COORDINATOR

    CoordinatorSprintController->>SprintRepository: findById(sprintId)
    SprintRepository-->>CoordinatorSprintController: Optional<Sprint>

    alt sprint not found
        CoordinatorSprintController-->>Coordinator: 404 {"message": "Sprint not found"}
    end

    CoordinatorSprintController->>SprintService: getSprintOverview(sprintId)

    Note over SprintService: termId resolved server-side — never from client
    SprintService->>ProjectGroupRepository: findByTermIdAndStatusIn(termId, [TOOLS_BOUND, ADVISOR_ASSIGNED])
    Note over ProjectGroupRepository: ALL groups — not filtered to any advisor
    ProjectGroupRepository-->>SprintService: List<ProjectGroup>

    loop for each group
        SprintService->>SprintTrackingLogRepository: findByGroupIdAndSprintId(group.id, sprintId)
        SprintTrackingLogRepository-->>SprintService: List<SprintTrackingLog>

        SprintService->>ScrumGradeRepository: findByGroupIdAndSprintId(group.id, sprintId)
        ScrumGradeRepository-->>SprintService: Optional<ScrumGrade>

        Note over SprintService: advisorName = group.getAdvisor()?.getName() — null for TOOLS_BOUND groups
        Note over SprintService: gradeSubmitted = grade.isPresent() — pointA_grade/pointB_grade null if absent
    end

    SprintService-->>CoordinatorSprintController: List<SprintGroupOverview>
    CoordinatorSprintController-->>Coordinator: 200 { sprintId, groups: [{ groupId, groupName, advisorName, totalIssues, mergedPRs, aiCounts, gradeSubmitted, pointA_grade, pointB_grade }, ...] }
```
